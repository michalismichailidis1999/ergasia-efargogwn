<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="michailidismichalis.com.ergasiasxolis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ErgasiaSxolis">
        <activity android:name=".progress.EatedMealAdapter"></activity>
        <activity android:name=".progress.ProgressActivity" />
        <activity android:name=".NextMeal.SaveMealActivity" />
        <activity android:name=".NextMeal.NextMealActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".Auth.RegisterUserCredentialsActivity" />
        <activity android:name=".Auth.RegisterUserInfoActivity" />
        <activity android:name=".Auth.LoginActivity" />
        <activity android:name=".InitialScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>